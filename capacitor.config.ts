import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pwa.webradio',
  appName: 'PWA webradio',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
